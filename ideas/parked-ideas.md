# Parked ideas

Deferred from the 2026-09-01 review; not scheduled. Revisit once the species count grows.

- **"Similar species" strip on a species page.** Show the 3–5 nearest morphotypes (the
  distance already exists in `lib/visual-index-layout.ts` `morphologyDistance`) as cards
  under the profile, so a researcher unsure of an identification can jump to look-alikes.
- **Faceted identification helper.** Narrow the index by observable traits (organization,
  colour, cell shape, size range) using the normalized morphology in
  `lib/morphology-normalize.ts`; a guided alternative to the morphotype grid.
